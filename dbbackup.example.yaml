targets:
  - name: local_sqlite
    db_type: sqlite
    file_path: ./sample.db

  - name: local_mysql
    db_type: mysql
    host: localhost
    port: 3306
    username: root
    password: ${MYSQL_PASSWORD}
    database: testdb